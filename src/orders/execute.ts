import { ILimitOrder } from "../models/models";

export function executeOrders(orders: ILimitOrder[]): Promise<void> {

  // TODO
  if (orders.length > 1) {



  } else if (orders.length === 1) {



  }

  return;
}